# SNP Core API

```bash
export JWT_SECRET=long-random-production-secret
export API_PORT=8090
node server.mjs
```

Per-user isolation under `data/users/<user_id>/`. Flutter default `API_BASE_URL=http://127.0.0.1:8090`.
